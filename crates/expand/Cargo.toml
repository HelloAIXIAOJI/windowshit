[package]
name = "expand"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Rust 重写的 Windows expand.exe（跨平台整活）"

[[bin]]
name = "expand"
path = "src/main.rs"

[dependencies]
windowshit-args = { path = "../args" }
cab = "0.6"
